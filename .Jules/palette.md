## 2024-05-23 - Icon-Only Buttons
**Learning:** Icon-only buttons (like traffic light controls) are invisible to screen readers without ARIA labels, creating a confused experience.
**Action:** Always add explicit `aria-label` to buttons that rely solely on iconography, especially terminal window controls.
