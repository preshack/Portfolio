# Palette's Journal

## 2024-05-22 - [Window Controls & Accessibility]
**Learning:** Icon-only buttons (like window controls) are invisible to screen readers without ARIA labels. Users expect standard window control behaviors (Red=Close/Reload, Yellow=Min, Green=Max).
**Action:** Always add `aria-label` to icon buttons and align behavior with platform conventions where possible.
